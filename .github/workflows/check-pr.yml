name: Checks

on:
  pull_request:

jobs:
  build:
    name: Check lint
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: just check

  tests:
    name: Check project
    runs-on: self-hosted
    needs: build
    steps:
      - uses: actions/checkout@v2
      - run: just test